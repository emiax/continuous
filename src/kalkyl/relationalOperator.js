define(function (require) {
    var exports = require('exports');
    var q = require('quack');
    var BinaryOperator = require('./binaryOperator');
  
    return q.createAbstractClass(BinaryOperator, [InnerNode], {
        
    });
});
