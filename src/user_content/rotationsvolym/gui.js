define(function(require) {
    var state = require('./state');
    var gui = {
        layout: 'card_layout',
        template: 'template.html',
        title: 'Rotationsvolym'
    };

    return gui;
});
